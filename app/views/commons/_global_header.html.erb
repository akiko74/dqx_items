    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <a class="brand" href="/"><%= t("app_name") %></a>
          <div class="pull-right">
            <% if user_signed_in? -%>
              <%= link_to 'ログアウト', destroy_user_session_path, :class => 'btn'  %>
            <% else -%>
              <%= link_to '登録', new_user_registration_path, :class => 'btn btn-primary'  %>
              <%= link_to 'ログイン', new_user_session_path, :class => 'btn'  %>
            <% end -%>
          </div>
        </div>
<% if admin_signed_in? %>
        <ul class="nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              レシピ管理
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'レシピ追加', new_recipe_path() %></li>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              素材アイテム管理
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to '素材アイテム追加', new_item_path() %></li>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              アカウント
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to 'ログアウト', destroy_admin_session_path() %></li>
            </ul>
          </li>
        </ul>
<% end %>
      </div>
    </div>

